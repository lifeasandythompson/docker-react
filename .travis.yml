language: generic

sudo: required
services:
  - docker

before_install:
  - docker build -t lifeasandythompson/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true n582346/docker-react npm run test